# arpscan-rs

A Rust-based command-line tool for performing ARP scans to discover devices on a network. It identifies IP addresses, MAC addresses, and manufacturers using OUI data.

## Installation
Install via RPM or compile from source.

## Usage
Run `sudo arpscan-rs` and enter a CIDR range (e.g., `192.168.1.0/24`).
